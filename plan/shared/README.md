# Shared Plans

This directory contains shared planning namespaces that are not owned by a
single co-worker.

Use shared plans for integration work, merge coordination, cross-owner
decisions, or repository-level maintenance that affects multiple personal
workstreams.

Shared plans must still declare `Owned files`, `Read-only files`, validation
steps, and commit intent.
