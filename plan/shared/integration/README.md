# Integration Plans

Use this directory for shared integration work:

```text
plan/shared/integration/<date-goal-slug>/plan.md
```

Integration plans are Coordinator-owned unless a specific merge owner is named.
They should reconcile outputs from personal plan namespaces and update the
global `plan/log.md` after validation.
