# Wiki Overview And Log Contract

This is a compatibility entry for historical links.

The current owner for `wiki/overview.md`, `wiki/log.md`, overview refresh
triggers, log update triggers, and wiki-facing closure surface requirements is:

```text
rules/wiki-surface-workflow.md
```

Old responsibility summary:

- keep `wiki/overview.md` as a concise current corpus map
- keep `wiki/log.md` as a short chronological wiki-facing record
- refresh overview when scope, source groups, chapter structure, coverage, or
  review themes change
- update the wiki log when accepted wiki pages, reports, review state, index,
  or overview changes
- keep reports linked instead of copied

Do not add new normative semantics here. Update `wiki-surface-workflow.md`
instead.
