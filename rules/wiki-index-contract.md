# Wiki Index Contract

This is a compatibility entry for historical links.

The current owner for `wiki/index.md` navigation behavior, entry shape,
sections, special pages, validation targets, and pass conditions is:

```text
rules/wiki-surface-workflow.md
```

Old responsibility summary:

- keep `wiki/index.md` as the primary content navigation entry
- group source/deck pages, chapter pages, reports, and optional pages clearly
- index accepted pages and important active reports
- avoid indexing every raw file, packet anchor, evidence row, or claim row
- treat missing or stale entries as lint or validation findings

Do not add new normative semantics here. Update `wiki-surface-workflow.md`
instead.
