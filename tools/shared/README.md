# Shared Tool Conventions

This directory records shared conventions for Phase 6 checker tools.

It is not a library implementation yet. Future tools may move common functions
here after at least two checkers need the same behavior.

Current shared conventions:

- `report-conventions.md`: report shape, status vocabulary, and exit-code
  semantics for checker tools.
